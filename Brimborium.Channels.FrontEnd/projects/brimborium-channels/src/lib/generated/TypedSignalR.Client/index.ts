/* THIS (.ts) FILE IS GENERATED BY TypedSignalR.Client.TypeScript */
/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import type { HubConnection, IStreamResult, Subject } from '@microsoft/signalr';
import type { IBCVisualizationHub, IBCVisualizationReveiver } from './Brimborium.Channels.Hubs';
import type { BCDescriptionGraph, BCLogRecord } from '../Brimborium.Channels';


// components

export type Disposable = {
    dispose(): void;
}

export type HubProxyFactory<T> = {
    createHubProxy(connection: HubConnection): T;
}

export type ReceiverRegister<T> = {
    register(connection: HubConnection, receiver: T): Disposable;
}

type ReceiverMethod = {
    methodName: string,
    method: (...args: any[]) => void
}

class ReceiverMethodSubscription implements Disposable {

    public constructor(
        private connection: HubConnection,
        private receiverMethod: ReceiverMethod[]) {
    }

    public readonly dispose = () => {
        for (const it of this.receiverMethod) {
            this.connection.off(it.methodName, it.method);
        }
    }
}

// API

export type HubProxyFactoryProvider = {
    (hubType: "IBCVisualizationHub"): HubProxyFactory<IBCVisualizationHub>;
}

export const getHubProxyFactory = ((hubType: string) => {
    if(hubType === "IBCVisualizationHub") {
        return IBCVisualizationHub_HubProxyFactory.Instance;
    }
}) as HubProxyFactoryProvider;

export type ReceiverRegisterProvider = {
    (receiverType: "IBCVisualizationReveiver"): ReceiverRegister<IBCVisualizationReveiver>;
}

export const getReceiverRegister = ((receiverType: string) => {
    if(receiverType === "IBCVisualizationReveiver") {
        return IBCVisualizationReveiver_Binder.Instance;
    }
}) as ReceiverRegisterProvider;

// HubProxy

class IBCVisualizationHub_HubProxyFactory implements HubProxyFactory<IBCVisualizationHub> {
    public static Instance = new IBCVisualizationHub_HubProxyFactory();

    private constructor() {
    }

    public readonly createHubProxy = (connection: HubConnection): IBCVisualizationHub => {
        return new IBCVisualizationHub_HubProxy(connection);
    }
}

class IBCVisualizationHub_HubProxy implements IBCVisualizationHub {

    public constructor(private connection: HubConnection) {
    }

    public readonly getDescriptionGraph = async (graphId: string): Promise<BCDescriptionGraph> => {
        return await this.connection.invoke("GetDescriptionGraph", graphId);
    }

    public readonly descriptionGraphChannel = (): IStreamResult<BCDescriptionGraph> => {
        return this.connection.stream("DescriptionGraphChannel");
    }

    public readonly logRecordChannel = (): IStreamResult<BCLogRecord> => {
        return this.connection.stream("LogRecordChannel");
    }
}


// Receiver

class IBCVisualizationReveiver_Binder implements ReceiverRegister<IBCVisualizationReveiver> {

    public static Instance = new IBCVisualizationReveiver_Binder();

    private constructor() {
    }

    public readonly register = (connection: HubConnection, receiver: IBCVisualizationReveiver): Disposable => {

        const __onJoin = () => receiver.onJoin();
        const __onLeave = () => receiver.onLeave();
        const __onMessage = (...args: [BCLogRecord[]]) => receiver.onMessage(...args);

        connection.on("OnJoin", __onJoin);
        connection.on("OnLeave", __onLeave);
        connection.on("OnMessage", __onMessage);

        const methodList: ReceiverMethod[] = [
            { methodName: "OnJoin", method: __onJoin },
            { methodName: "OnLeave", method: __onLeave },
            { methodName: "OnMessage", method: __onMessage }
        ]

        return new ReceiverMethodSubscription(connection, methodList);
    }
}

